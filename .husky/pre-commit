#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Ensure file headers, lint, and typechecks pass before committing
npm run check:headers || exit 1
npm run lint --silent || exit 1
npm run typecheck --silent || exit 1
