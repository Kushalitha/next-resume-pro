# Next Resume Pro v2.0.0
# Author: Kushalitha Maduranga
# Year: 2026
#
# License:
# - Code (TypeScript, JavaScript, build scripts): MIT License
# - UI / Design (CSS, layout, visual components): CC BY 4.0
# 	Attribution Required
#
# Repository:
# https://github.com/Kushalitha

# ============================================
# PRODUCTION CONFIGURATION
# ============================================
# Copy this file to .env.local for development
# Copy to .env.production for production deployment
# NEVER commit actual .env files to git!

# Site URL (required for SEO, sitemaps, metadata)
SITE_URL=https://your-domain.com
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# ============================================
# GITHUB INTEGRATION (Optional)
# ============================================
GITHUB_USERNAME=your-github-username
# Personal Access Token - get from: https://github.com/settings/tokens
# Permissions needed: public_repo (read-only)
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# CONTACT FORM EMAIL (Optional)
# ============================================
# Where contact form submissions are sent
CONTACT_TO_EMAIL=you@example.com
CONTACT_REPLY_TO=you@example.com

# Enable SMTP email sending (disabled by default for security)
# Set to 'true' only when SMTP is properly configured
# EMAIL_ENABLED=false

# SMTP Configuration (required when EMAIL_ENABLED=true)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# CONTACT_FROM="Your Name <no-reply@example.com>"

# ============================================
# REDIS (Optional - for rate limiting)
# ============================================
# Recommended for production to prevent abuse
# REDIS_URL=redis://:password@hostname:6379/0
# REDIS_URL=redis://localhost:6379

# ============================================
# DEBUG & LOGGING (Development Only)
# ============================================
# SECURITY WARNING: NEVER enable these in production!
# Enable server-side logging to logs/debug.log
# DEBUG_LOG=false

# Allow downloading log dumps via /api/debug/download
# DANGER: Only enable in local development
# DEBUG_ALLOW_READ=false

# Allow client-side error reporting to server
# NEXT_PUBLIC_DEBUG_LOG=false

# Automatically create timestamped dump files on errors
# DEBUG_AUTO_DUMP=false

# Capture uncaught exceptions and unhandled rejections
# DEBUG_CAPTURE_UNCAUGHT=false

# Optional: Protect debug endpoints with a secret
# DEBUG_WRITE_SECRET=your-random-secret-here
# DEBUG_READ_SECRET=your-random-secret-here

# Optional: Custom log directory and size limit
# DEBUG_LOG_DIR=./logs
# DEBUG_MAX_BYTES=5242880

# ============================================
# SECURITY CHECKLIST FOR PRODUCTION
# ============================================
# ✅ Set SITE_URL to your actual domain
# ✅ Configure EMAIL_ENABLED=false unless SMTP is tested
# ✅ Set DEBUG_LOG=false
# ✅ Set DEBUG_ALLOW_READ=false
# ✅ Set NEXT_PUBLIC_DEBUG_LOG=false
# ✅ Add REDIS_URL for rate limiting
# ✅ Use strong, unique passwords for SMTP
# ✅ Never commit .env files to version control
# ✅ Use environment variables from your hosting provider
# ✅ Review and rotate GITHUB_TOKEN regularly
# ✅ Enable HTTPS on your domain


